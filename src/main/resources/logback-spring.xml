<configuration debug="false" scan="true" scanPeriod="3 seconds">
    <contextName>logback</contextName>
    
    <property name="path" value="./" />
    <property name="maxHistory"  value="5"/>
    <property name="maxFileSize" value="30MB"/>
     
    <!-- 加载spring变量 -->
    <springProperty scope="context" name="app.name" source="spring.application.name"/>
 
    <appender name="console" class="ch.qos.logback.core.ConsoleAppender">
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>INFO</level>
        </filter>
        <encoder>
            <pattern>%date %level [%thread] %logger{36} [%file : %line] %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>    
    
    <appender name="log_file" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${path}/${app.name}.log</file>
        <append>true</append>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!-- 每天一归档 -->
            <fileNamePattern>${path}/${app.name}.log.%d{yyyy-MM-dd}-%i.zip</fileNamePattern>
            <maxHistory>${maxHistory}</maxHistory>
            <timeBasedFileNamingAndTriggeringPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">
                <maxFileSize>${maxFileSize}</maxFileSize>
            </timeBasedFileNamingAndTriggeringPolicy>
        </rollingPolicy>
        <encoder>
            <pattern>%date %level [%thread] %logger{36} [%file : %line] %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>
 
    <logger name="com.cpit.icp" level="INFO">
    </logger>
 
    <root>
        <level value="WARN" />
        <appender-ref ref="log_file" addtivity="false"/>
        <appender-ref ref="console" addtivity="false"/>
    </root>
</configuration>